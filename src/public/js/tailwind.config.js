tailwind.config = {
    darkMode: "class",
    theme: {
        extend: {
            colors: {
                primary: "var(--primary)",
                secondary: 'var(--secondary)',
                tertiary: 'var(--tertiary)',

            },
        },
    },
};
